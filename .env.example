# Este app usa as configurações do Firebase em firebase-applet-config.json.
# Não coloque senhas ou chaves privadas neste arquivo.
#
# Para rodar localmente:
# 1. Configure o Firebase Authentication com login Google.
# 2. Adicione localhost nos domínios autorizados do Firebase.
# 3. Publique as regras seguras do firestore.rules.
